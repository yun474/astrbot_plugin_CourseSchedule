# Changelog

## [2.1.0] - 2026-09-22

### 新增

- `/查看课表`、`/查看明日课表` 支持查看他人课表：可带 `@某人`、`<@!openid>`、openid / QQ 号或群友昵称，不带参数仍是查自己。
- LLM 工具 `course_schedule_query` 新增 `user_id` 参数，用于查询指定群友，不传则查询当前提问的用户。

仅能查询在当前群已绑定课表的人，跨群与私聊之间互相隔离。

## [2.0.0] - 2026-09-22

### 新增

- 官bot（QQ 官方机器人）Markdown 模式：课表、排行、帮助菜单可用 Markdown 卡片 + 按钮展示，每个指令可在配置中单独开关，发送失败自动回退为图片 / 文本。
- `/课表帮助` 指令：功能菜单，官bot 下为带「导入课表 / 今日课表 / 明日课表 / 群友在上什么课」按钮的卡片。
- `/关联课表 绑定码`：用绑定码把已绑定的课表复用到其他群或私聊，不用重复上传；不带参数时显示自己的绑定码。
- `/解绑课表`：解除当前会话的绑定。
- `/绑定课表 昵称`：绑定时可指定显示昵称（官bot 拿不到昵称时使用）。
- WakeUp 口令导入同步上游 [#8](https://github.com/advent259141/astrbot_plugin_CourseSchedule/pull/8) 的 6.x 协议实现（antispam + getv2），并补上单双周课程支持。
- 官bot 群聊里绑定提示会艾特发起人；其他平台群聊使用 At 组件。
- 官bot 下按 openid 获取头像（`q.qlogo.cn/qqapp/{appid}/{openid}`）。

### 变更

- LLM 工具精简为一个 `course_schedule_query(scope)`，`scope` 为 today / tomorrow / week。工具只返回文本（含每节课起止时间与当前日期时间），由模型自行判断课程状态，不再在工具内直接发图。
- 移除按关键词注入系统提示、以及从 LLM 请求中删除 Python / 联网工具的逻辑。
- 群友课表图片中当天无课的群友显示为「无课程」，不再显示为「已结束」。
- `metadata.yaml` 仓库地址指向本仓库，声明支持平台与最低 AstrBot 版本（>= 4.22.0）。

### 修复

- 课表图片临时文件不再残留（交给 AstrBot 事件结束后清理）。
- 上传非 `.ics` 或无法解析的文件会被拒绝并删除，不再导致全群查询报错。
- `.ics` 中缺少 `DTEND`（仅有 `DURATION`）或个别事件损坏时不再整份解析失败。
- 解析缓存改为每个文件只保留当前周，不再随时间累积。
- 课程开始的那一秒被判定为「已结束」、整 60 分钟显示为「60 分钟」的边界问题。
